apply plugin: "org.jlleitschuh.gradle.ktlint" // Version should be inherited from parent

ktlint{
    version = "0.42.1"

    reporters {
        reporter "plain"
        reporter "checkstyle"
        reporter "html"
    }

    outputColorName = "RED"
}